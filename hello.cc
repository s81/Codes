
// auto natural_numbers = std::views::iota(1);

// numbers | std::views::take(10)

// auto multiple_by_two = [] (int number) {return number * 2;};

// auto even_numbers = natural_numbers | std::views::transform(multiple_by_two);

// std::accumulate(std::begin(salaries_in_cents), std::end(salaries_in_cents), 0,  std::plus{});

// std::map<std::string, int> employees{};

// for( std::string name : names) employees[name] = 0;

// for(auto [name, salary] : employees) std::cout << name << " - " << salary << '\n' ;
